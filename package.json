{
  "name": "auditoria-wcag-v2",
  "version": "1.2.0",
  "type": "module",
  "description": "Auditoría de accesibilidad automatizada con Cypress + axe-core + Puppeteer + ExcelJS + GitHub Actions (IAAP/W3C)",
  "scripts": {
    "preaudit": "node -e \"if (!require('fs').existsSync('./scripts/urls.json')) { console.error('❌ No se encontró scripts/urls.json. Ejecuta npm run crawl:js'); process.exit(1) } else { console.log('✅ URLs detectadas, listo para auditar.') }\"",
    "crawl": "node crawler/crawl.js",
    "crawl:js": "node crawler/crawl-puppeteer.mjs",
    "crawl:full": "MAX_DEPTH=5 CRAWL_DELAY=1200 node crawler/crawl-puppeteer.mjs",

    "validate:urls": "ts-node scripts/validate-urls.ts",

    "audit:sitemap": "cypress run --spec cypress/e2e/accesibilidad-sitemap.cy.js --browser chrome --headless",
    "audit:interactiva": "cypress run --spec cypress/e2e/accesibilidad-interactiva.cy.js --browser chrome --headless",
    "audit:dev": "cypress open --browser chrome",
    "audit:debug": "cypress run --spec cypress/e2e/accesibilidad-sitemap.cy.js --browser chrome --headed",

    "merge": "node scripts/merge-results.mjs",
    "fix": "node scripts/fix-json.cjs",
    "summary": "node scripts/post-merge-summary.cjs",
    "summary:analyze": "CRITICAL_MAX=5 SERIOUS_MAX=50 npm run summary",
    "report": "node --experimental-specifier-resolution=node scripts/export-to-xlsx.mjs && ls -lh auditorias/",
    "report:debug": "node --trace-warnings scripts/export-to-xlsx.mjs",
    "quality": "node scripts/quality-gate.cjs",

    "audit:all": "npm run validate:urls && npm run crawl:js && npm run audit:sitemap && npm run audit:interactiva && npm run merge && npm run report",
    "audit:full": "npm run preaudit && npm run crawl:full && npm run validate:urls && npm run audit:sitemap && npm run audit:interactiva && npm run merge && npm run summary && npm run report && npm run quality",
    "audit:local": "CRITICAL_MAX=9999 SERIOUS_MAX=9999 npm run audit:full",
    "audit:ci": "CRITICAL_MAX=0 SERIOUS_MAX=5 npm run audit:full"
  },
  "dependencies": {
    "archiver": "^7.0.0",
    "axe-core": "^4.10.0",
    "axios": "^1.7.2",
    "chalk": "^5.3.0",
    "cheerio": "^1.0.0-rc.12",
    "cypress": "^13.17.0",
    "cypress-axe": "^1.5.0",
    "date-fns": "^3.6.0",
    "exceljs": "^4.4.0",
    "fs-extra": "^11.2.0",
    "open": "^10.2.0",
    "puppeteer": "^23.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "accesibilidad",
    "wcag",
    "cypress",
    "axe-core",
    "puppeteer",
    "exceljs",
    "auditoria",
    "github-actions",
    "automatizacion",
    "en301549",
    "iaap",
    "w3c"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/suso77/auditoria-wcag-v2"
  },
  "bugs": {
    "url": "https://github.com/suso77/auditoria-wcag-v2/issues"
  },
  "homepage": "https://github.com/suso77/auditoria-wcag-v2#readme",
  "author": "Suso (Ilúmina Media)",
  "license": "MIT"
}





