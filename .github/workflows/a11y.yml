name: ♿ Auditoría de Accesibilidad – a11y CI
run-name: "♿ Auditoría WCAG (${{ github.event.inputs.site_url || 'default' }})"

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: "URL base del sitio a auditar"
        required: false
        default: "https://www.hiexperience.es"
  push:
    branches: [ main, master, principal ]
  pull_request:
  # schedule:
  #   - cron: "0 3 * * 1"  # (opcional) ejecución semanal automática

jobs:
  a11y:
    runs-on: ubuntu-latest

    env:
      SITE_URL: ${{ github.event.inputs.site_url || vars.SITE_URL || 'https://www.hiexperience.es' }}
      LC_ALL: C.UTF-8
      LANG: C.UTF-8
      CRITICAL_MAX: 5
      SERIOUS_MAX: 20
      TZ: Europe/Madrid
      NODE_ENV: production
      NODE_OPTIONS: "--max-old-space-size=4096 --experimental-specifier-resolution=node --no-warnings"

      TS_NODE_TRANSPILE_ONLY: true
      TS_NODE_PROJECT: tsconfig.json

      CYPRESS_screenshotsFolder: auditorias/capturas
      CYPRESS_video: false
      CYPRESS_screenshotOnRunFailure: true

    steps:
      - name: 🧩 Checkout del repositorio
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "**/package-lock.json"

      - name: 🧠 Cachear dependencias de Cypress y Puppeteer
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/Cypress
            ~/.cache/puppeteer
          key: ${{ runner.os }}-a11y-${{ hashFiles('package-lock.json', 'cypress.config.*') }}

      - name: 🧹 Limpiar resultados antiguos
        run: |
          rm -rf auditorias/* || true
          mkdir -p auditorias/capturas auditorias/logs

      - name: 📦 Instalar dependencias
        run: |
          echo "📦 Instalando dependencias..."
          npm ci || npm install
          mkdir -p scripts
          if [ ! -f scripts/urls.json ]; then echo "[]" > scripts/urls.json; fi
          echo "✅ Dependencias instaladas correctamente."

      - name: 🧩 Verificar ts-node
        run: npx ts-node --version || npm install ts-node typescript --no-save

      - name: 🧩 Verificar entorno de Cypress
        run: |
          echo "::group::🧩 Verificando instalación de Cypress"
          npx cypress verify || (echo "⚠️ Reinstalando Cypress..." && npx cypress install)
          echo "::endgroup::"

      - name: 🧾 Validar entorno base
        run: node scripts/check-env.cjs

      - name: 🧠 Mostrar entorno detectado
        run: |
          echo "::group::🧠 Variables activas"
          node -p "({ SITE_URL: process.env.SITE_URL, NODE_ENV: process.env.NODE_ENV, TZ: process.env.TZ })"
          echo "::endgroup::"

      - name: 🔍 Validar listado de URLs inicial
        run: |
          echo "::group::🔍 Validando scripts/urls.json"
          npx ts-node scripts/validate-urls.ts || echo "⚠️ Se generará en el siguiente paso"
          echo "::endgroup::"

      - name: 🌐 Rastreo de URLs
        timeout-minutes: 15
        run: |
          set -e
          echo "::group::🌍 Rastreo en $SITE_URL"
          npm run crawl:js
          npx ts-node scripts/validate-urls.ts
          echo "::endgroup::"

      - name: ♿ Auditoría de accesibilidad – Sitemap
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          install-command: npm ci
          command: |
            npx cypress run --browser chrome --headless --config-file cypress.config.cjs --spec "cypress/e2e/sitemap/**/*.cy.js" || npm run audit:sitemap
        env:
          SITE_URL: ${{ env.SITE_URL }}

      - name: 🧠 Auditoría de accesibilidad – Componentes interactivos
        continue-on-error: true
        run: |
          npx cypress run --browser chrome --headless --config-file cypress.config.cjs --spec "cypress/e2e/interactiva/**/*.cy.js" || npm run audit:interactiva || echo "⚠️ No hay specs interactivas."

      - name: 🏷️ Añadir campo "origen" a los resultados
        run: node scripts/tag-origen.cjs

      - name: 🧩 Combinar resultados
        run: |
          set -e
          node scripts/merge-results.mjs
          echo "✅ Archivo combinado generado correctamente."

      - name: 🚦 Quality Gate – WCAG
        continue-on-error: true
        run: npm run quality || echo "⚠️ Quality Gate con advertencias"

      - name: 📸 Generar evidencias visuales (capturas WCAG)
        if: always()
        run: |
          echo "::group::📸 Generando evidencias visuales"
          node --max-old-space-size=4096 --experimental-specifier-resolution=node scripts/capture-evidence.mjs
          echo "::endgroup::"
          echo "✅ Capturas y metadatos generados en auditorias/capturas/"
        env:
          PUPPETEER_SKIP_DOWNLOAD: true

      - name: 📊 Exportar informe Excel + ZIP (IAAP / W3C)
        if: always()
        run: |
          set -e
          echo "::group::📊 Generando informe profesional IAAP / W3C"
          node --max-old-space-size=4096 --experimental-specifier-resolution=node scripts/export-to-xlsx.mjs
          echo "::endgroup::"
          echo "✅ Informe Excel y ZIP generados correctamente."
          ls -lh auditorias/

      - name: 🧾 Generar resumen ejecutivo (Markdown)
        if: always()
        run: |
          node scripts/generate-summary.mjs auditorias/results-merged-*.json > auditorias/Resumen-WCAG.md || echo "⚠️ No se pudo generar resumen."

      - name: 🔍 Validar informe generado
        if: always()
        run: |
          if [ ! -f auditorias/Informe-WCAG-Profesional.xlsx ]; then
            echo "❌ No se generó el informe Excel."
            exit 1
          fi
          echo "✅ Informe Excel detectado correctamente."

      - name: 📤 Subir artefactos finales
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "WCAG-Informe-${{ github.run_number }}-${{ github.event.inputs.site_url || 'default' }}"
          path: |
            auditorias/results-merged-*.json
            auditorias/Informe-WCAG-Profesional.xlsx
            auditorias/Informe-WCAG.zip
            auditorias/Resumen-WCAG.md
            auditorias/capturas/**/*.png
          retention-days: 30

      - name: ✅ Resumen final de ejecución
        if: always()
        run: |
          echo "==============================================="
          echo "♿ RESUMEN FINAL DE LA AUDITORÍA"
          echo "==============================================="
          echo "🌍 Sitio auditado: $SITE_URL"
          echo "📊 Informe generado: auditorias/Informe-WCAG-Profesional.xlsx"
          echo "📸 Capturas incluidas en ZIP"
          echo "🚦 Quality Gate: Critical <= $CRITICAL_MAX, Serious <= $SERIOUS_MAX"
          echo "✅ Pipeline completado correctamente."
