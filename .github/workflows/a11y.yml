name: ♿ Auditoría de Accesibilidad – Ilúmina Audit IAAP PRO
run-name: "♿ Auditoría WCAG (${{ github.event.inputs.site_url || 'default' }})"

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: "URL base del sitio a auditar"
        required: false
        default: "https://www.hiexperience.es"
  push:
    branches: [ main, master, principal ]
  pull_request:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  a11y:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    env:
      SITE_URL: ${{ github.event.inputs.site_url || vars.SITE_URL || 'https://www.hiexperience.es' }}
      BUILD_DATE: ${{ github.run_id }}-${{ github.run_number }}
      TZ: Europe/Madrid
      NODE_ENV: production
      NODE_OPTIONS: "--max-old-space-size=4096 --no-warnings"
      CYPRESS_screenshotsFolder: auditorias/capturas
      CYPRESS_video: false
      CYPRESS_screenshotOnRunFailure: true
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/google-chrome
      CI: true

    steps:
      - name: 🧩 Checkout del repositorio
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📦 Instalar dependencias IAAP PRO
        run: |
          echo "📦 Instalando dependencias IAAP PRO..."
          npm install --include=dev
          npm install --save-dev cypress cypress-axe cypress-real-events @bahmutov/cypress-esbuild-preprocessor esbuild puppeteer pa11y fs-extra exceljs json2csv
          npx cypress verify
          mkdir -p scripts auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          [ ! -f scripts/urls.json ] && echo '[]' > scripts/urls.json
          echo "✅ Entorno IAAP PRO preparado correctamente."

      - name: 🧹 Limpiar resultados anteriores
        run: |
          rm -rf auditorias/* public/* || true
          mkdir -p auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          echo "🧹 Carpetas de auditoría limpias."

      - name: 🌍 Rastreo automático IAAP PRO
        timeout-minutes: 30
        run: |
          echo "::group::🌍 Rastreo IAAP PRO"
          npm run crawl:auto || echo "⚠️ Rastreo automático fallido, usando fallback..."
          npm run validate:urls || echo "⚠️ Validación no crítica"
          echo "::endgroup::"

      - name: ♿ Auditoría WCAG – Sitemap Híbrido (axe + Pa11y)
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          command: >
            npx cypress run --e2e --browser chrome --headless
            --config-file cypress.config.mjs
            --spec "cypress/e2e/accesibilidad-sitemap-hibrido.cy.js"
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}

      - name: ♿ Auditoría WCAG – Interactiva Híbrida (axe + Pa11y)
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          command: >
            npx cypress run --e2e --browser chrome --headless
            --config-file cypress.config.mjs
            --spec "cypress/e2e/accesibilidad-interactiva-hibrida.cy.js"
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}

      - name: ♿ IAAP PRO — Exportar revisiones manuales (needs_review)
        if: always()
        run: |
          echo "::group::♿ Exportando revisiones manuales..."
          node scripts/export-needs-review.mjs || echo "⚠️ No se encontraron revisiones manuales."
          echo "::endgroup::"

      - name: ♿ IAAP PRO — Combinar resultados (axe + Pa11y)
        if: always()
        run: |
          echo "::group::♿ Fusión completa IAAP PRO..."
          node scripts/merge-auditorias.mjs || echo "⚠️ Merge IAAP no crítico."
          echo "::endgroup::"

      - name: ♿ IAAP PRO — Exportar informes XLSX / CSV / PDF / HTML
        if: always()
        run: |
          echo "::group::📊 Exportando informes IAAP PRO..."
          mkdir -p auditorias/reportes
          if [ -f auditorias/reportes/merged-results.json ]; then
            node scripts/export-to-xlsx.mjs || echo "⚠️ XLSX no crítico."
            node scripts/export-to-csv.mjs || echo "⚠️ CSV no crítico."
            node scripts/export-to-pdf.mjs || echo "⚠️ PDF no crítico."
            node scripts/generate-dashboard-html.mjs "${{ env.BUILD_DATE }}" || echo "⚠️ Dashboard no crítico."
            node scripts/generate-summary.mjs auditorias/reportes/merged-results.json > auditorias/reportes/merged-summary.md || echo "⚠️ Summary no crítico."
          else
            echo "⚠️ No hay merged-results.json, se omite exportación."
          fi
          echo "::endgroup::"

      - name: 📦 Crear y subir ZIP IAAP PRO completo
        if: always()
        run: |
          ZIP_NAME="IAAP-PRO-${{ env.BUILD_DATE }}.zip"
          ZIP_PATH="auditorias/${ZIP_NAME}"
          zip -r "${ZIP_PATH}" auditorias/* || true
          echo "✅ ZIP IAAP PRO generado: ${ZIP_PATH}"

      - name: 📤 Subir artefactos IAAP PRO (ZIP + XLSX + CSV + PDF)
        uses: actions/upload-artifact@v4
        with:
          name: "IAAP-PRO-${{ github.run_number }}"
          path: |
            auditorias/IAAP-PRO-*.zip
            auditorias/reportes/*.xlsx
            auditorias/reportes/*.csv
            auditorias/reportes/*.pdf
          retention-days: 30

      - name: 🚀 Publicar Dashboard en GitHub Pages
        if: success() && (hashFiles('auditorias/reportes/merged-results.json') != '')
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          keep_files: true
          full_commit_message: |
            🚀 Publicación automática IAAP PRO v5.0
            - Fecha: ${{ github.event.head_commit.timestamp }}
            - Run ID: ${{ github.run_id }}
            - Build Date: ${{ env.BUILD_DATE }}
