name: ♿ Auditoría de Accesibilidad – Ilúmina Audit IAAP PRO
run-name: "♿ Auditoría WCAG (${{ github.event.inputs.site_url || 'default' }})"

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: "URL base del sitio a auditar"
        required: false
        default: "https://www.hiexperience.es"
  push:
    branches: [ main, master, principal ]
  pull_request:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  a11y:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      SITE_URL: ${{ github.event.inputs.site_url || vars.SITE_URL || 'https://www.hiexperience.es' }}
      BUILD_DATE: ${{ github.run_id }}-${{ github.run_number }}
      TZ: Europe/Madrid
      NODE_ENV: production
      NODE_OPTIONS: "--max-old-space-size=4096 --no-warnings"
      CYPRESS_screenshotsFolder: auditorias/capturas
      CYPRESS_video: false
      CYPRESS_screenshotOnRunFailure: true
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/google-chrome
      CI: true

    steps:
      # =====================================================
      # 🧩 Setup base
      # =====================================================
      - name: 🧩 Checkout del repositorio
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 🧠 Cachear dependencias pesadas (Cypress + Puppeteer)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/Cypress
            ~/.cache/puppeteer
          key: ${{ runner.os }}-a11y-v4.13-${{ hashFiles('package-lock.json') }}

      # =====================================================
      # 📦 Instalación + verificación crítica IAAP PRO
      # =====================================================
      - name: 📦 Instalación y verificación de dependencias IAAP PRO
        run: |
          echo "📦 Instalando dependencias base..."
          set +e
          npm ci --include=dev || npm install --include=dev
          npm rebuild || true

          echo "🔍 Verificando dependencias críticas..."
          for pkg in cypress cypress-axe cypress-real-events @bahmutov/cypress-esbuild-preprocessor esbuild; do
            if ! npm list --depth=1 $pkg >/dev/null 2>&1; then
              echo "⚙️ Instalando $pkg..."
              npm install --save-dev $pkg
            else
              echo "✅ $pkg ya está instalado."
            fi
          done

          echo "🧹 Forzando compilación previa para evitar errores de bundling..."
          npx cypress cache path
          npx cypress verify || npm rebuild cypress || true

          echo "📂 Creando estructura de carpetas..."
          mkdir -p scripts auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          [ ! -f scripts/urls.json ] && echo "[]" > scripts/urls.json
          echo "✅ Dependencias IAAP PRO instaladas correctamente."

      # =====================================================
      # 🧪 Validar entorno Cypress (pre-bundling)
      # =====================================================
      - name: 🧪 Validar entorno y preprocesador de Cypress
        run: |
          echo "🔍 Validando entorno Cypress..."
          if [ ! -f "cypress.config.cjs" ]; then
            echo "❌ ERROR: No se encuentra cypress.config.cjs en el repositorio."
            exit 1
          fi

          echo "🧠 Validando módulo @bahmutov/cypress-esbuild-preprocessor..."
          if ! npm list @bahmutov/cypress-esbuild-preprocessor >/dev/null 2>&1; then
            echo "⚙️ Instalando preprocesador..."
            npm install --save-dev @bahmutov/cypress-esbuild-preprocessor esbuild
          else
            echo "✅ Preprocesador detectado correctamente."
          fi

          echo "🧠 Validando cypress-axe y cypress-real-events..."
          node -e "require('cypress-axe'); require('cypress-real-events'); console.log('✅ Módulos cargados correctamente');"

          echo "📄 Validando cypress.config.cjs..."
          node -e "require('./cypress.config.cjs'); console.log('✅ Configuración Cypress válida.');"
          echo "🔢 Versión de Cypress:"
          npx cypress version
          echo "✅ Entorno Cypress listo para ejecutar auditorías."

      # =====================================================
      # ♻️ Limpieza previa
      # =====================================================
      - name: 🧹 Limpiar resultados anteriores
        run: |
          rm -rf auditorias/* public/* || true
          mkdir -p auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          echo "🧹 Carpetas limpias y preparadas."

      # =====================================================
      # 🌐 Rastreo automático IAAP PRO
      # =====================================================
      - name: 🌍 Rastreo automático IAAP PRO (v4.13.5)
        timeout-minutes: 30
        run: |
          echo "::group::🌍 Rastreo IAAP PRO"
          npm run crawl:auto || echo "⚠️ Rastreo automático fallido, intentando fallback..."
          npm run validate:urls || echo "⚠️ Validación no crítica"
          echo "::endgroup::"
        env:
          SITE_URL: ${{ env.SITE_URL }}

      # =====================================================
      # ♿ Auditorías principales
      # =====================================================
      - name: ♿ Auditoría WCAG – Sitemap
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          command: >
            npx cypress run --e2e --browser chrome --headless
            --config-file cypress.config.cjs
            --spec cypress/e2e/accesibilidad-sitemap.cy.js
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}

      - name: ♿ Auditoría WCAG – Interactiva
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          command: >
            npx cypress run --e2e --browser chrome --headless
            --config-file cypress.config.cjs
            --spec cypress/e2e/accesibilidad-interactiva.cy.js
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}

      # =====================================================
      # 🧩 MERGE + EXPORTACIÓN IAAP PRO
      # =====================================================
      - name: ♿ IAAP PRO — Combinar y exportar informes
        if: always()
        run: |
          echo "::group::♿ Combinando resultados IAAP PRO..."
          node scripts/merge-auditorias.mjs || echo "⚠️ Merge IAAP no crítico."
          echo "::endgroup::"

          if [ -f auditorias/reportes/merged-results.json ]; then
            echo "::group::📊 Exportando informes IAAP PRO..."
            mkdir -p auditorias/reportes
            node scripts/export-to-xlsx.mjs auditorias/reportes/merged-results.json || echo "⚠️ XLSX no crítico."
            node scripts/export-to-csv.mjs auditorias/reportes/merged-results.json || echo "⚠️ CSV no crítico."
            node scripts/export-to-pdf.mjs auditorias/reportes/merged-results.json || echo "⚠️ PDF no crítico."
            node scripts/generate-summary.mjs auditorias/reportes/merged-results.json > auditorias/reportes/merged-summary.md || echo "⚠️ Summary no crítico."
            echo "::endgroup::"
          else
            echo "⚠️ No hay merged-results.json, saltando exportación."
          fi

      # =====================================================
      # 🌐 Dashboard + Artefactos + Publicación
      # =====================================================
      - name: 🌐 Generar Dashboard IAAP PRO
        run: |
          BUILD_PATH="public/auditorias/${BUILD_DATE}"
          REPORTES_PATH="auditorias/reportes"
          mkdir -p "${BUILD_PATH}"
          cp -r "${REPORTES_PATH}"/* "${BUILD_PATH}/" 2>/dev/null || true
          echo "✅ Dashboard IAAP PRO generado correctamente."

      - name: 📦 Crear y subir ZIP IAAP PRO completo
        if: always()
        run: |
          ZIP_NAME="IAAP-PRO-${{ env.BUILD_DATE }}.zip"
          ZIP_PATH="auditorias/${ZIP_NAME}"
          zip -r "${ZIP_PATH}" auditorias/* || true
          echo "✅ ZIP IAAP PRO generado: ${ZIP_PATH}"

      - name: 📤 Subir artefacto IAAP PRO
        uses: actions/upload-artifact@v4
        with:
          name: "IAAP-PRO-${{ github.run_number }}"
          path: auditorias/IAAP-PRO-*.zip
          retention-days: 30

      - name: 🚀 Publicar en GitHub Pages
        if: success() && (hashFiles('auditorias/reportes/merged-results.json') != '')
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          keep_files: true
          full_commit_message: |
            🚀 Publicación automática IAAP PRO v4.13.5
            - Fecha: ${{ github.event.head_commit.timestamp }}
            - Run ID: ${{ github.run_id }}
            - Build Date: ${{ env.BUILD_DATE }}
