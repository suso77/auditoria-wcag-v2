name: ♿ Auditoría de Accesibilidad – Ilúmina Audit IAAP PRO v6.5
run-name: "♿ Auditoría WCAG (${{ github.event.inputs.site_url || 'default' }})"

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: "URL base del sitio a auditar"
        required: false
        default: "https://www.hiexperience.es"
  push:
    branches: [ main, master, principal ]
  pull_request:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  a11y:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    env:
      SITE_URL: ${{ github.event.inputs.site_url || vars.SITE_URL || 'https://www.hiexperience.es' }}
      BUILD_DATE: ${{ github.run_id }}-${{ github.run_number }}
      TZ: Europe/Madrid
      LANG: es
      NODE_ENV: development
      NODE_OPTIONS: "--max-old-space-size=4096 --no-warnings"
      MAX_URLS: 80
      MAX_DEPTH: 5
      CRAWL_DELAY: 1000
      CYPRESS_screenshotsFolder: auditorias/capturas
      CYPRESS_video: false
      CYPRESS_screenshotOnRunFailure: true
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/google-chrome
      CHROME_BIN: /usr/bin/google-chrome
      CHROME_PATH: /usr/bin/google-chrome
      CI: true

    steps:
      # ======================================================
      # 🧩 Repositorio y Node.js
      # ======================================================
      - name: 🧩 Checkout del repositorio
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      # ======================================================
      # 🌐 Verificar disponibilidad del sitio
      # ======================================================
      - name: 🔍 Verificar disponibilidad del sitio
        run: |
          echo "🔎 Verificando que $SITE_URL responde correctamente..."
          STATUS=$(curl -o /dev/null -s -w "%{http_code}" -L "$SITE_URL")
          if [ "$STATUS" != "200" ]; then
            echo "❌ El sitio no respondió con HTTP 200 (recibido: $STATUS)"
            exit 1
          fi
          echo "✅ Sitio accesible (HTTP 200 OK)."

      # ======================================================
      # 🧠 Instalar Chrome Headless
      # ======================================================
      - name: 🧠 Instalar entorno Chrome Headless
        run: |
          sudo apt-get update && sudo apt-get install -y google-chrome-stable
          export PUPPETEER_SKIP_DOWNLOAD=true
          echo "CHROME_BIN=$(which google-chrome)" >> $GITHUB_ENV
          echo "CHROME_PATH=$(which google-chrome)" >> $GITHUB_ENV
          echo "✅ Chrome disponible en: $(which google-chrome)"

      # ======================================================
      # 📦 Instalar dependencias IAAP PRO
      # ======================================================
      - name: 📦 Instalar dependencias IAAP PRO
        run: |
          echo "📦 Instalando dependencias IAAP PRO..."
          npm ci --include=dev || npm install --include=dev || true
          npm install puppeteer --silent || true
          npm install --save-dev cypress cypress-axe cypress-real-events @bahmutov/cypress-esbuild-preprocessor esbuild pa11y fs-extra exceljs json2csv chalk --silent || true
          npx cypress verify || echo "⚠️ Cypress verificado con advertencias (no crítico)."
          mkdir -p scripts auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          [ ! -f scripts/urls.json ] && echo '[]' > scripts/urls.json
          echo "✅ Entorno IAAP PRO preparado correctamente."

      # ======================================================
      # 🧹 Limpieza previa
      # ======================================================
      - name: 🧹 Limpieza resultados anteriores
        run: |
          rm -rf auditorias/* public/* || true
          mkdir -p auditorias/{capturas,auditoria-sitemap,auditoria-interactiva,reportes} public/auditorias
          echo "🧹 Limpieza completada."

      # ======================================================
      # 🌍 Rastreo automático IAAP PRO (forzado idioma /es/)
      # ======================================================
      - name: 🌍 Rastreo automático IAAP PRO (solo idioma /es/)
        timeout-minutes: 30
        run: |
          echo "::group::🌍 Rastreo IAAP PRO"
          echo "🌐 Forzando idioma /es/ y límite máximo de 80 URLs"
          export LANG_FILTER="/es/"
          export MAX_URLS=80
          echo "🔧 SITE_URL=$SITE_URL"
          echo "🔧 LANG_FILTER=$LANG_FILTER"
          echo "🔧 MAX_URLS=$MAX_URLS"
          npm run crawl:auto || echo "⚠️ Rastreo automático fallido, usando fallback..."
          npm run validate:urls || echo "⚠️ Validación no crítica"
          echo "::endgroup::"
        env:
          SITE_URL: ${{ env.SITE_URL }}
          MAX_URLS: 80
          LANG: es
          LANG_FILTER: "/es/"

      # ======================================================
      # 🧾 Verificar URLs generadas
      # ======================================================
      - name: 🧾 Verificar URLs generadas
        run: |
          if [ ! -s scripts/urls.json ]; then
            echo "❌ No se generó scripts/urls.json o está vacío."
            exit 1
          fi
          COUNT=$(jq length scripts/urls.json)
          FILTERED=$(jq '[.[] | select(.url | contains("/es/"))] | length' scripts/urls.json)
          if [ "$COUNT" -gt 80 ]; then
            echo "❌ Se superó el límite de 80 URLs ($COUNT detectadas)."
            exit 1
          fi
          if [ "$FILTERED" -eq 0 ]; then
            echo "❌ No se detectaron URLs que contengan /es/."
            exit 1
          fi
          echo "✅ scripts/urls.json contiene $FILTERED URLs válidas con /es/."

      # ======================================================
      # 🔍 Verificación de scripts Cypress
      # ======================================================
      - name: 🔍 Verificar que Sitemap no ejecuta Pa11y
        run: |
          echo "🔍 Revisando accesibilidad-sitemap-hibrido.cy.js..."
          if grep -Eiq "pa11y" cypress/e2e/accesibilidad-sitemap-hibrido.cy.js; then
            echo "❌ Error: El archivo Sitemap contiene referencias a Pa11y."
            exit 1
          fi
          echo "✅ OK: Sitemap usa solo axe-core."

      # ======================================================
      # ♿ Auditorías WCAG IAAP PRO
      # ======================================================

      - name: 🧾 Verificar existencia de specs
        run: |
          for file in cypress/e2e/accesibilidad-sitemap-hibrido.cy.js cypress/e2e/accesibilidad-interactiva-hibrida.cy.js; do
            if [ ! -f "$file" ]; then
              echo "❌ Archivo de test no encontrado: $file"
              exit 1
            fi
            echo "✅ Archivo detectado: $file"
          done

      - name: ♿ Auditoría WCAG – Sitemap (solo axe-core)
        uses: cypress-io/github-action@v6
        with:
          command: >
            npx cypress run --browser chrome --headless
            --spec cypress/e2e/accesibilidad-sitemap-hibrido.cy.js
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}
          AUDIT_ENGINE: axe-core
          AUDIT_SOURCE: sitemap

      - name: ♿ Auditoría WCAG – Interactiva (Pa11y + fallback axe-core)
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          command: >
            npx cypress run --browser chrome --headless
            --spec cypress/e2e/accesibilidad-interactiva-hibrida.cy.js
        env:
          CYPRESS_baseUrl: ${{ env.SITE_URL }}
          AUDIT_ENGINE: pa11y
          AUDIT_FALLBACK: axe-core
          AUDIT_SOURCE: interactiva

      # ======================================================
      # ♿ Merge + Verificación de Metadatos IAAP PRO
      # ======================================================
      - name: ♿ Fusionar resultados (merge-auditorias v6.5)
        run: node scripts/merge-auditorias.mjs

      - name: 🧩 Verificar integridad de metadatos (verify-metadata v6.5)
        run: |
          echo "🔍 Validando metadatos IAAP PRO..."
          node scripts/verify-metadata.mjs
        continue-on-error: false

      # ======================================================
      # 📊 Exportar informes IAAP PRO
      # ======================================================
      - name: 📊 Exportar informes IAAP PRO
        if: always()
        run: |
          if [ -f auditorias/reportes/merged-results.json ]; then
            node scripts/generate-summary.mjs
          else
            echo "⚠️ No se encontró merged-results.json, se omite exportación."
          fi

      # ======================================================
      # 🔍 Verificación final del pipeline
      # ======================================================
      - name: 🔍 Verificar pipeline IAAP PRO
        if: always()
        run: node scripts/verify-pipeline.mjs || echo "⚠️ Advertencias menores."

      # ======================================================
      # 📦 ZIP y artefactos
      # ======================================================
      - name: 📦 Comprimir resultados IAAP PRO
        if: always()
        run: |
          ZIP_NAME="IAAP-PRO-v6.5-${{ env.BUILD_DATE }}.zip"
          zip -r "auditorias/${ZIP_NAME}" auditorias/* || true

      - name: 📤 Subir artefactos IAAP PRO
        uses: actions/upload-artifact@v4
        with:
          name: "IAAP-PRO-v6.5-${{ github.run_number }}"
          path: |
            auditorias/IAAP-PRO-v6.5-*.zip
            auditorias/reportes/*.xlsx
            auditorias/reportes/*.json
            auditorias/Resumen-WCAG.md
          retention-days: 30

      # ======================================================
      # 🚀 Publicar Dashboard en GitHub Pages
      # ======================================================
      - name: 🚀 Publicar Dashboard IAAP PRO v6.5
        if: always() && hashFiles('auditorias/reportes/merged-results.json') != ''
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          keep_files: true
          full_commit_message: |
            🚀 Publicación automática IAAP PRO v6.5
            - Fecha: ${{ github.event.head_commit.timestamp }}
            - Run ID: ${{ github.run_id }}
            - Build Date: ${{ env.BUILD_DATE }}
