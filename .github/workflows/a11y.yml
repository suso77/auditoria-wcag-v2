name: ♿ Auditoría de Accesibilidad – a11y CI
run-name: "♿ Auditoría WCAG (${{ github.event.inputs.site_url || 'default' }})"

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: "URL base del sitio a auditar"
        required: false
        default: "https://www.hiexperience.es"
  push:
    branches: [ main, master, principal ]
  pull_request:
  # ⚡ (Opcional) Ejecutar automáticamente cada lunes a las 03:00 UTC
  # schedule:
  #   - cron: "0 3 * * 1"

jobs:
  a11y:
    runs-on: ubuntu-latest

    env:
      SITE_URL: ${{ github.event.inputs.site_url || vars.SITE_URL || 'https://www.hiexperience.es' }}
      LC_ALL: C.UTF-8
      LANG: C.UTF-8
      CRITICAL_MAX: 5
      SERIOUS_MAX: 20
      TZ: Europe/Madrid
      NODE_ENV: production
      NODE_OPTIONS: "--experimental-specifier-resolution=node"

    steps:
      # 1️⃣ Clonar repositorio
      - name: 🧩 Checkout del repositorio
        uses: actions/checkout@v4

      # 2️⃣ Configurar Node.js
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "**/package-lock.json"

      # ✅ 2.1 Cachear dependencias de Cypress y Puppeteer
      - name: 🧠 Cachear dependencias de Cypress y Puppeteer
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/Cypress
            ~/.cache/puppeteer
          key: ${{ runner.os }}-a11y-cache-${{ hashFiles('package-lock.json', 'package.json') }}

      # 🧹 Limpieza previa
      - name: 🧹 Limpiar resultados antiguos
        run: |
          rm -rf auditorias/* || true
          mkdir -p auditorias

      # 3️⃣ Instalar dependencias
      - name: 📦 Instalar dependencias
        run: |
          echo "📦 Instalando dependencias..."
          npm ci || npm install
          echo "✅ Dependencias instaladas correctamente."
          npm list --depth=0 || true

      # 4️⃣ Verificar entorno de Cypress
      - name: 🧩 Verificar entorno de Cypress
        run: |
          echo "::group::🧩 Verificando instalación de Cypress"
          npx cypress verify || (echo "⚠️ Cypress verification fallida, reinstalando..." && npx cypress install)
          echo "::endgroup::"

      # 5️⃣ Rastreo de URLs
      - name: 🌐 Rastreo de URLs con Puppeteer
        run: |
          set -e
          echo "::group::🌍 Rastreo de URLs"
          echo "🌍 Iniciando rastreo en $SITE_URL"
          npm run crawl:js
          if [ ! -s scripts/urls.json ]; then
            echo "❌ No se generó scripts/urls.json. Abortando auditoría."
            exit 1
          fi
          echo "✅ Rastreo completado. Archivo: scripts/urls.json"
          echo "::endgroup::"

      # 6️⃣ Auditoría general (Sitemap)
      - name: ♿ Auditoría de accesibilidad – Sitemap
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          install-command: npm ci
          command: npm run audit:sitemap
        env:
          SITE_URL: ${{ env.SITE_URL }}

      # 7️⃣ Auditoría interactiva (automática según idioma)
      - name: 🧠 Auditoría de accesibilidad – Componentes interactivos
        continue-on-error: true
        run: |
          echo "🧠 Detectando script de auditoría interactiva..."
          if npm run | grep -q "audit:interactiva"; then
            echo "🌐 Ejecutando auditoría interactiva (modo español)"
            npm run audit:interactiva
          elif npm run | grep -q "audit:interactive"; then
            echo "🌐 Ejecutando auditoría interactiva (modo inglés)"
            npm run audit:interactive
          else
            echo "⚠️ No se encontró ningún script interactivo (audit:interactiva o audit:interactive)."
            echo "🟡 Saltando auditoría interactiva..."
          fi
        env:
          SITE_URL: ${{ env.SITE_URL }}

      # 🏷️ Etiquetar resultados
      - name: 🏷️ Añadir campo "origen" a los resultados
        run: node scripts/tag-origen.cjs

      # 8️⃣ Combinar resultados
      - name: 🧩 Combinar resultados de auditorías
        run: |
          set -e
          echo "::group::🔄 Combinando resultados de auditorías"
          npm run merge || npm run fix
          echo "::endgroup::"
          echo "✅ Archivo combinado generado correctamente."

      # 9️⃣ Validar archivo combinado
      - name: 🔍 Validar archivo combinado
        run: |
          set -e
          FILE=$(ls auditorias/results-merged-*.json 2>/dev/null | tail -n 1 || true)
          if [ -z "$FILE" ]; then
            echo "❌ No se encontró results-merged.json, intentando regenerar..."
            npm run merge || npm run fix
          elif [ ! -s "$FILE" ]; then
            echo "⚠️ Archivo vacío, intentando reparar..."
            npm run fix || true
          else
            echo "✅ Archivo válido detectado: $FILE"
          fi

      # 🔟 Resumen rápido de severidades
      - name: 🧮 Resumen rápido de severidades
        run: |
          echo "::group::📊 Resumen de severidades detectadas"
          jq -r '[.[].violations[].impact] | group_by(.) | map({nivel: .[0], total: length})' auditorias/results-merged-*.json || echo "⚠️ jq no disponible o sin datos"
          echo "::endgroup::"

      # 11️⃣ Quality Gate
      - name: 🚦 Quality Gate – WCAG
        run: |
          echo "==============================================="
          echo "🚦 INICIO DEL CONTROL DE CALIDAD"
          echo "==============================================="
          npm run quality || true
          echo "==============================================="
          echo "✅ FIN DEL CONTROL DE CALIDAD"
          echo "==============================================="
        continue-on-error: true

      # 12️⃣ Exportar informe profesional
      - name: 📊 Exportar informe Excel + ZIP (IAAP / W3C)
        if: always()
        run: |
          set -e
          echo "::group::📊 Generando informe profesional IAAP / W3C"
          FILE=$(ls auditorias/results-merged-*.json 2>/dev/null | tail -n 1 || true)
          if [ -z "$FILE" ]; then
            echo "❌ No se encontró archivo de resultados combinados. Abortando exportación."
            exit 1
          fi
          echo "📄 Archivo combinado detectado: $FILE"
          node --experimental-specifier-resolution=node scripts/export-to-xlsx.mjs
          echo "::endgroup::"
          echo "✅ Informe Excel y ZIP generados correctamente."
          echo "📂 Contenido de /auditorias:"
          ls -lh auditorias/ || echo "⚠️ Carpeta no encontrada."

      # 13️⃣ Subir artefactos finales
      - name: 📤 Subir artefactos finales
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "WCAG-Informe-${{ github.run_number }}-${{ github.event.inputs.site_url || 'default' }}"
          path: |
            auditorias/results-merged-*.json
            auditorias/Informe-WCAG-Profesional.xlsx
            auditorias/Informe-WCAG.zip
            auditorias/*-evidencias/**
          retention-days: 30

      # 15️⃣ Resumen final
      - name: ✅ Resumen de ejecución
        if: always()
        run: |
          echo "==============================================="
          echo "♿ RESUMEN FINAL DE LA AUDITORÍA"
          echo "==============================================="
          echo "🌍 Sitio auditado: $SITE_URL"
          echo "📊 Resultados combinados + informe Excel profesional generados"
          echo "🚦 Quality Gate: Critical <= $CRITICAL_MAX, Serious <= $SERIOUS_MAX"
          echo "✅ Pipeline finalizado correctamente."
